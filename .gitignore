# Live credentials and local overrides — never commit.
.env

# Built image artifact (~190 MB). Produced by `docker save`, never committed --
# a fresh clone builds the image instead. Ship it out of band if needed.
*.tar

# Editor / OS noise
.DS_Store
.cursor/
**/.DS_Store
**/__pycache__/
**/*.pyc
